{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "description": "Authoritative Colyseus game server. One room = one match.",
  "main": "build/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node build/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.76",
    "colyseus": "^0.16.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
