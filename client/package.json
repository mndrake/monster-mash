{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phaser 3 + Vite PWA client. Renders the arena from server state.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "colyseus.js": "^0.16.22",
    "nipplejs": "^1.0.4",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
